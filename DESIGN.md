# 누적 막대그래프 디자인 규칙

이 문서는 SigmaIntel Production 화면에서 확정한 누적 막대그래프 규칙이다. 새 누적 막대그래프도 같은 비교 문법과 밀도를 사용한다.

## 1. 지원 화면과 구성

- 공식 검증 화면은 일반 데스크톱 `1440 × 900px`이다.
- SigmaIntel Production은 가로와 세로 스크롤 없이 한 화면에 들어와야 한다.
- Counterpoint Weekly는 `페이지 제목 → 필터 → OEM × Region 매트릭스 → 지역 선택 → 58:42 차트` 순서를 사용하며, 가로 스크롤은 금지하고 세로 스크롤은 허용한다.
- 기본 순서는 `페이지 제목 → Executive Summary → 필터 → 2열 차트`다.
- Executive Summary는 제목 하나와 1~3개의 독립 불릿으로 구성한다.
- Executive Summary 제목은 `16px / 24px`, 700 굵기를 사용한다.
- 요약 본문은 `14px / 20px`, 400 굵기를 사용하고 강조 수치만 600으로 올린다.

## 2. 2열 차트 레이아웃

- 좌우 카드는 `58fr 42fr`로 나눠 14개 분기 차트의 가로 공간을 우선 확보하고, 제목 높이와 하단선을 맞춘다.
- 데스크톱 본문 좌우 여백은 `24px`, Forecast 업체 범례의 최소 폭은 `96px`로 유지해 차트 영역을 우선 확보한다.
- 카드 배경은 `--mi-color-surface-soft`, 모서리는 `8px`, 안쪽 여백은 `22px 20px 18px`을 사용한다.
- 두 차트의 막대 기준선과 하단 축 차이는 `1px` 이하여야 한다.
- 차트 내부에는 가로 스크롤을 만들지 않는다.

## 3. 막대 폭과 간격

- 비교하는 두 차트에서 막대 하나의 폭은 같아야 한다.
- 막대 간격도 양쪽 모두 `6px`로 통일한다.
- 14개 분기 차트의 막대 폭을 기준값으로 사용한다.
- 왼쪽 플롯 내부 폭을 `W`, 좌우 여백을 각각 `5px`로 둘 때 막대 폭은 `(W - 10px - 13 × 6px) ÷ 14`다.
- 오른쪽 6개월 플롯은 위에서 계산한 막대 폭 6개와 간격 5개, 좌우 여백 각각 `3px`만 차지하도록 폭을 정한다.
- 기간 수가 달라도 막대를 임의로 늘리지 않는다. 남는 공간은 범례나 분석 정보에 사용한다.

## 4. 누적 막대 문법

- 각 막대는 `Total → 누적 구성값 → 기간 라벨` 순서로 읽힌다.
- Total은 막대 위에 `14px`, 600 이상 굵기로 표시한다.
- 양쪽 차트의 구성값은 모두 `12px / 16px`, 500 굵기를 사용한다.
- 기간 라벨은 모두 `12px / 16px`, 400 굵기를 사용한다.
- 수량 단위는 `Mu`, 값은 소수점 한 자리로 통일한다.
- 같은 차트의 모든 막대는 동일한 최대값과 높이 기준으로 정규화한다.
- 작은 조각도 숨기지 않는다. 글자를 표시할 수 없는 경우에만 연결선 또는 외부 라벨을 사용한다.

## 5. 색상과 Forecast

- 업체 순서는 `Huawei → Xiaomi → OPPO → vivo → Honor → Lenovo → Transsion`으로 고정한다.
- 색상은 순서대로 `--mi-chart-blue-1`부터 `--mi-chart-blue-7`까지 사용한다.
- 같은 업체는 모든 차트와 선택 상태에서 같은 색을 유지한다.
- Forecast 막대는 파란색 점선 윤곽과 `Forecast` 문구를 함께 사용한다.
- 상승은 파란색, 하락은 빨간색을 사용하며 방향 데이터에만 적용한다.

## 6. 범례

- 주 차트의 색상 범례는 필터 버튼과 결합한다.
- 상세 차트 범례는 차트 오른쪽에 세로로 배치한다.
- 범례는 `색상 표식 → 업체명 → 변화량` 순서로 표시한다.
- 상세 차트 범례는 `12px / 16px`, 400 굵기를 사용한다.
- 업체명과 변화량은 한 행에 두고 상승과 하락 부호를 생략하지 않는다.
- 범례 영역의 남는 가로 공간은 업체별 변화량을 비교하는 보조 시각화에만 사용할 수 있다.

## 7. 선택과 상태

- 분기 막대 전체를 버튼으로 만들고 선택 상태에 `aria-pressed`를 제공한다.
- 선택 막대는 내부 2px 파란 윤곽으로 표시해 레이아웃 폭을 바꾸지 않는다.
- 키보드 포커스는 `:focus-visible`에서 명확하게 보여야 한다.
- 업체 필터와 `ONLY` 선택은 왼쪽과 오른쪽 차트, Executive Summary에 동시에 반영한다.
- 분기를 바꿔도 막대 폭, 간격, 기준선, 색상 순서는 변하지 않아야 한다.

## 8. 타이포 위계

- 페이지 제목은 `32px / 40px`, 700 굵기를 사용한다.
- 카드 제목은 `20px / 28px`, 700 굵기를 사용한다.
- Executive Summary 제목은 `16px / 24px`, 700 굵기, 본문은 `14px / 20px`, 400 굵기를 사용한다.
- 일반 설명과 Total은 `14px`, 차트 값과 범례는 `12px`을 사용한다.
- 굵기는 `400 / 500 / 600 / 700` 네 단계만 사용한다.

## 9. 완료 조건

- 1440 × 900px에서 SigmaIntel은 페이지 스크롤이 없고, Weekly는 가로 스크롤만 없는가?
- 좌우 카드 폭과 하단선이 같은가?
- 왼쪽과 오른쪽 막대 하나의 폭 및 간격이 같은가?
- Total, 구성값, 기간 라벨이 잘리거나 겹치지 않는가?
- 14개 분기와 6개월 Forecast가 각각 한 줄을 유지하는가?
- 모든 선택 상태에서 데이터, 색상, 요약이 함께 갱신되는가?
- 콘솔 오류가 없는가?

## 10. Counterpoint Weekly 적용

- 화면 순서는 `페이지 제목 → Executive Summary → Reporting Week → OEM × Region 매트릭스 → Total/Region 선택 → 58:42 차트`다.
- Executive Summary는 현재 주차 데이터에서 생성한 2개 불릿만 사용한다.
- `Total`은 전체 시장 추이와 Region 누적 구성을, 개별 Region은 해당 지역 추이와 Vendor 누적 구성을 표시한다.
- 차트 kicker는 `12px / 16px`, 500 굵기, 파란색을 사용하고 제목은 `20px / 28px`, 700 굵기를 사용한다.
- 선형 차트 축과 범례, 누적 차트 구성값과 기간 라벨은 화면상 `12px / 16px`를 유지하고 Total은 `14px / 18px`, 600 굵기로 표시한다.
- 차트 표면은 `#f7f9fc`, 모서리는 `8px`, 좌우 차트는 같은 폭과 같은 plot 비율을 사용한다.
- OEM × Region 매트릭스는 `760px` 이내로 왼쪽 정렬하고 셀 텍스트는 범례와 같은 `12px / 16px`를 사용한다.
- Matrix의 지역 열은 해당 Region 차트로 이동한다. Vendor 행은 Vendor 필터 데이터가 추가되기 전까지 차트 필터로 사용하지 않는다.
